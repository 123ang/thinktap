# Backend Environment Variables

# Database
DATABASE_URL="postgresql://thinktap:changeme@localhost:5432/thinktap"

# JWT Configuration
JWT_SECRET="change-this-to-a-strong-random-secret-at-least-64-characters-long"
JWT_REFRESH_SECRET="change-this-to-another-strong-random-secret-at-least-64-characters"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Redis (optional - for scaling)
REDIS_URL="redis://localhost:6379"

# Server Configuration
NODE_ENV="development"
PORT=4004

# CORS
CORS_ORIGIN="http://localhost:3004,http://localhost:19006"

# Generate strong secrets using:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
